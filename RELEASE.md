# 릴리스(배포) 방법 — RELEASE.md

> **개발**(브랜치 → PR → 머지)은 `WORKFLOW.md`. 이 문서는 그 다음, **머지된 코드를 실제
> 사용자(maruvis.kr)에게 내보내는 절차**입니다. 배포 경험이 없어도 따라 할 수 있게 썼습니다.

---

## 딱 세 가지만 알면 됩니다

1. **`main` ≠ 운영 서버.** `main`은 "리뷰 끝나고 합쳐진 최신 코드"이고, 운영 서버는 그중
   **이름표가 붙은 버전**만 돌립니다. 머지했다고 사용자에게 바로 보이지 않습니다.
2. **이름표 = 태그.** `v0.2.0-alpha` 처럼 커밋 하나에 붙이는 이름입니다. 한 번 붙으면 절대
   안 움직입니다. 그래서 "지금 뭐가 떠 있어요?"에 `v0.2.0-alpha`라고 답할 수 있고, 문제가
   생기면 이전 이름표로 **되돌릴 수** 있습니다.
3. **되돌리기(롤백) = 이전 태그로 바꾸고 재기동.** 명령 하나, 30초쯤.

## 누가 무엇을 하나

| 누가 | 하는 일 | 어디서 |
|---|---|---|
| **진서** @JinnyLyn | PR 기술 검토(테스트·코드·보안)와 머지, **배포 명령 실행**, 롤백 | 서버 터미널 |
| **성원** @Sung1Lim | PR 제품 검토(요구사항·사용자 영향·운영 정책), **릴리스 발행 = 배포 승인**, 배포 후 확인 | GitHub 웹 |

한 사람이 혼자서는 사용자에게 보이는 것을 못 바꿉니다. 성원이 릴리스를 발행하지 않으면
배포할 태그가 없고, 진서가 배포 명령을 치지 않으면 서버는 그대로입니다.

## 폴더 셋 — 개발 / staging / 운영

서버에는 같은 레포가 **세 폴더**로 꺼내져 있습니다 (git worktree — 히스토리는 하나, 파일만 따로).

| 폴더 | 뭐가 있나 | 포트 / 주소 | 누가 건드리나 |
|---|---|---|---|
| `~/camchat` | **개발.** 브랜치 마음대로 | (서버 안 띄움 — 띄우려면 `env.local` 에 다른 포트; 기본 포트는 거부됨) | 사람·에이전트 |
| `~/camchat-staging` | 항상 `origin/main` 최신 — **머지한 걸 굴려보는 곳** | :8010/:3010 · `staging.maruvis.kr` | `staging.sh up\|down` 만 |
| `~/camchat-prod` | 항상 **릴리스 태그** — 사용자가 보는 것 | :8000/:3000 · `maruvis.kr` | `deploy.sh` 만 |

- 운영·staging 폴더에서는 **편집·커밋·브랜치 변경 금지.** 커밋은 훅이 막고(`.deploy-worktree` 마커),
  거기서 뭘 바꾸면 다음 `deploy.sh` 가 "커밋 안 된 수정" 으로 거부합니다. 개발은 `~/camchat` 에서.
- 개발 폴더에서 뭘 해도 사용자한테 영향 없습니다 — 운영은 다른 폴더의 다른 프로세스입니다.
- `.venv`(파이썬 라이브러리, 9.5 GB)는 셋이 **공유**합니다(심링크). 개발 폴더에서 `pip install` 로
  뭘 바꾸면 운영도 같이 바뀌니, 의존성 변경은 PR 로만 — 배포 때 `deploy.sh` 가 바뀐 만큼 설치합니다.
  staging 은 python 의존성을 설치하지 않습니다(운영 venv 를 건드리게 되므로) — 새 라이브러리가 필요한
  PR 은 staging 에서 그 부분이 안 돌 수 있고, 배포 때 설치됩니다.
- 처음 만드는 법: `MIGRATION_H100.md` 3-7 (`scripts/setup-worktrees.sh`).

## 버전 이름 규칙 — 단계(alpha → beta → 정식) + 숫자

태그 = `v0.<기능>.<수정>-<단계>`. 숫자는 단계가 바뀌어도 **이어서** 셉니다
(마지막 알파 `v0.3.0-alpha` → 첫 베타 `v0.4.0-beta`). 단계는 "누가 쓰고 있나"로 정합니다:

| 단계 | 언제 | 태그 | 누가 쓰나 |
|---|---|---|---|
| **alpha** | 지금 — 산학협력단 승인 전, 지인 대상 피드백 수집 | `v0.x.y-alpha` | 팀·지인 (실제 대상 아님) |
| **beta** | 산학협력단 승인 → 학교 공식 페이지에 링크된 뒤 | `v0.x.y-beta` | 재학생 = 실제 대상. 실로그·실피드백, 아직 "바뀔 수 있음" |
| **정식** | 산학협력단이 정식 채택(운영 정책·문의 창구까지 합의) | `v1.0.0` 부터, 접미사 없음 | 전교 |

### 숫자 올리는 법 — `vX.Y.Z`, 릴리스 하나에 한 칸만, 그 릴리스에서 제일 큰 변화 기준

| 자리 | 언제 올리나 | 누가 정하나 | 뒤 자리 |
|---|---|---|---|
| **X** | 학교에 보여줄 이정표. `v1.0.0` = 학교 최초 정식 배포. 이후엔 새 제품 면(공식 홈페이지 연동 등)이나 학교 쪽 링크·연동을 깨는 변경 | 진서 + 성원 함께 | Y, Z → 0 |
| **Y** | 사용자가 알아챌 새 것: 기능, 새 KB 문서 묶음, 답변 품질 개선. **기본값** | 성원 (발행 때) | Z → 0 |
| **Z** | 수정만: 버그, 오타, 틀린 답 교정, 설정. 핫픽스, **정상 운영** | 성원 (발행 때) | — |

| 상황 | 예 |
|---|---|
| 첫 릴리스 | `v0.1.0-alpha` (2026-09-10 기준 운영 코드) |
| 기능 추가 | `v0.1.0-alpha` → `v0.2.0-alpha` |
| 버그 수정만 | `v0.2.0-alpha` → `v0.2.1-alpha` |
| 단계 전환 (학생 공개) | `v0.3.0-alpha` → `v0.4.0-beta` |
| 학교 정식 배포 | `v0.6.2-beta` → `v1.0.0` |
| 정식 후 수정 / 기능 / 이정표 | `v1.0.1` / `v1.1.0` / `v2.0.0` |

번호는 건너뛰지도, 다시 쓰지도 않습니다. 진서가 "릴리스 준비됐어요" 할 때 번호를 제안하고,
성원이 위 표로 확인해 최종 이름을 정합니다. 헷갈리면 Y.
alpha·beta 릴리스는 GitHub 에서 **Set as a pre-release** 를 켭니다 — 정식 `v1.0.0` 부터 끕니다.

---

## 진서: 머지한 것 굴려보기 (staging, 몇 분)

PR 이 머지되면 사용자한텐 아직 아무 일도 없습니다. 실제로 돌려 봐야 아는 것(답변 품질, 화면,
속도)은 staging 에서 봅니다:

```bash
cd ~/camchat
./scripts/staging.sh up       # origin/main 최신을 :8010/:3010 에 띄움 (프론트 빌드 포함, 몇 분)
#   → https://staging.maruvis.kr 에서 굴려보기. 성원에게도 이 주소로 확인 부탁
./scripts/staging.sh status   # 어느 커밋이 떠 있는지, main 보다 뒤졌는지, 헬스
./scripts/staging.sh down     # 다 봤으면 내림 (켜 있을 때만 RAM 2.4 GB 씀)
```

문제가 있으면 고치는 PR → 머지 → `staging.sh up` 다시. 운영은 그동안 그대로입니다.
staging 은 LLM(ollama)을 운영과 같이 쓰므로 답변 속도는 운영과 같고, 자동 복구는 없습니다
(죽으면 `up` 으로 다시).

## 성원: 릴리스 발행 (GitHub 웹, 5분)

**0.** 진서가 알립니다: "릴리스 준비됐어요 — `v0.2.0-alpha`, 포함 PR #a #b #c, staging 에 떠 있어요."

**1. PR 확인 + staging.** 각 PR 본문의 **제품 검증** 칸(요구사항 일치 / 사용자 영향 /
운영 정책)이 채워져 있는지 보고, `staging.maruvis.kr` 에서 직접 눌러 봅니다 — 사용자에게 보이는
변경이면 무엇이 어떻게 바뀌었는지 눈으로 확인. 애매하면 PR에 댓글로 물어보고 **발행을 미루는 게
정상**입니다. 발행 **직전**에 진서에게 "staging 이 main 최신인가요?" 한 번 더 — 그 사이 Dependabot
이 자동 머지한 PR 이 끼어들 수 있어서(`staging.sh status` 로 확인).

**2. 릴리스 페이지.** `github.com/JinnyLyn/bufs-chatbot-v2` → 오른쪽 **Releases** →
**Draft a new release**.

**3. 태그 이름.** "Choose a tag"에 새 이름(예 `v0.2.0-alpha`) 입력 → **Create new tag on
publish** 클릭. Target 은 `main` 그대로.

**4. 내용 자동 채우기.** **Generate release notes** 버튼 → 지난 릴리스 이후 머지된 PR 목록이
자동으로 들어갑니다. 진서가 알려준 PR 번호와 맞는지 눈으로 한 번 비교합니다.

**5. 체크리스트.** 아래를 설명란 맨 위에 붙여넣고 확인한 항목의 `[ ]` 를 `[x]` 로 고칩니다
(릴리스 설명의 체크박스는 **클릭이 안 됩니다** — 텍스트를 고치는 겁니다):

```markdown
## 릴리스 확인 (@Sung1Lim)
- [ ] 포함된 PR 모두 제품 검증 완료
- [ ] 사용자에게 보이는 변화 파악함 (없으면 "없음")
- [ ] 운영 정책과 충돌 없음
- [ ] 배포 후 maruvis.kr 에서 질문 3개 정상 답변 (배포 후에 체크)

Approved by: 성원
```

"배포 기록"(Version / Commit / Released / Deployed by / Previous version / Rollback target)은 배포가
끝나면 `deploy.sh` 가 릴리스 설명에 **자동으로 채워 넣습니다** — 손으로 쓰지 않아도 됩니다. 이 줄들이
"누가 언제 무엇을 승인·배포했나"의 기록입니다.

**6. Set as a pre-release** 체크 (alpha·beta 동안은 항상).

**7. Publish release.** 이 순간 태그가 만들어지고 진서가 배포할 수 있게 됩니다.
**이 버튼이 "배포 승인"입니다.** 누르기 전엔 아무것도 사용자에게 나가지 않습니다.

**8. 배포 후 확인.** 진서가 "떴어요" 하면 `maruvis.kr` 에서 질문 3개(예: 수강신청 기간,
휴학 절차, 장학금 종류)를 해보고 답이 정상인지 봅니다. 이상하면 진서에게 **"롤백"** 한마디.
정상이면 릴리스 페이지 오른쪽 위 연필(**Edit release**) → 마지막 줄을 `- [x] 배포 후 …` 로 고치고 →
**Update release**.

## 진서: 배포 (서버 터미널, 2분)

```bash
cd ~/camchat-prod                   # 운영 폴더 (개발 폴더 ~/camchat 아님)
./scripts/deploy.sh status          # 지금 뭐가 떠 있는지 + 새 태그가 있는지
./scripts/deploy.sh v0.2.0-alpha     # 바뀌는 커밋 목록 보여주고 y/N → 체크아웃 → 재기동 → /health 확인
```

끝나면 `[done]   v0.2.0-alpha (abc1234) 운영 중.` 가 찍힙니다. 성원에게 "떴어요".
그 아래 `[record]` 블록(Version / Commit / Released / Deployed by / Previous version / Rollback
target)이 찍히고, **같은 내용이 GitHub 릴리스 설명에 자동으로 기입됩니다**(`gh`, 태그는 안 건드려서
태그 규칙에 안 걸림). "기입함" 대신 "손으로 붙여 넣으세요" 가 뜨면 그때만 Edit release 로 붙여 넣기.
`Approved by` 는 릴리스를 발행한 GitHub 계정, `Deployed by` 는 서버의 `gh` 에 로그인된 GitHub 계정으로 자동 기입됩니다(서버 계정 team_b 는 공용이라 안 씀). 다른 이름을 쓰려면 `scripts/env.local` 에 `export DEPLOY_BY=진서` 한 줄.

`requirements.txt` 나 `frontend/package-lock.json` 이 바뀐 릴리스는 `deploy.sh` 가 **설치까지**
합니다(`pip install` / `npm ci`, torch 계열은 지금 버전 고정). 설치가 실패하면 서버는 건드리지 않고
체크아웃만 되돌립니다. 지금 체크아웃에 맞춰 강제로 다시 설치하려면 `./scripts/deploy.sh deps`.

**문제가 생기면:**

```bash
./scripts/deploy.sh rollback        # 직전 버전으로. 묻지 않고 바로. ~30초
```

배포 스크립트가 재기동 뒤 백엔드가 응답하지 않는 것을 감지하면 **자동으로 이전 버전으로
돌아갑니다.** 프론트 빌드가 실패한 경우엔 서버를 건드리지 않고 체크아웃만 되돌리고, 백엔드는
떴는데 LLM(ollama) 확인만 실패한 경우엔 배포를 유지한 채 경고만 냅니다(재기동해도 ollama 가
고쳐지진 않으니). `rollback` 은 운영 폴더에 커밋 안 된 수정이 있어도 멈추지 않고 `git stash` 로
치워둔 뒤 진행합니다(복구: `git stash pop`). 어느 경우든 `./scripts/deploy.sh status` 로
확인하는 습관을.

명령 전체:

| 명령 | 뜻 |
|---|---|
| `deploy.sh v0.2.0-alpha` | 그 태그를 배포 (`--yes` 면 안 물어봄, `--dry-run` 이면 검사만) |
| `deploy.sh rollback [태그]` | 직전(또는 지정한) 버전으로 |
| `deploy.sh status` | 떠 있는 버전 / 체크아웃된 버전 / 실제 도는 프로세스 / 헬스 / 점검 모드 |
| `deploy.sh tags` | 릴리스 태그 목록(최신순), 떠 있는 것 표시 |
| `deploy.sh maint on\|off` | 점검 모드 (아래) |
| `deploy.sh deps` | 현재 체크아웃의 의존성 강제 설치 (npm ci + pip, torch 고정) |

## 점검 모드 — 오래 내려야 할 때만

```bash
./scripts/deploy.sh maint on     # 사용자에겐 "점검 중" 안내 페이지, 헬스체크는 자동 재기동 안 함
# ... 인덱스 재생성, 서버 작업 ...
./scripts/deploy.sh maint off
```

- 보통 배포(30초 안팎)엔 **필요 없습니다.** 그 사이엔 Cloudflare Worker 가 알아서 장애 안내
  페이지를 보여주고 서버가 올라오면 바로 복귀합니다.
- 사용자 화면 전환에 최대 60초 걸립니다(엣지 캐시).
- 서버에서 처음 한 번 Cloudflare 인증이 필요합니다. 화면 없는 서버라 브라우저 로그인 대신
  **API 토큰**이 편합니다: Cloudflare 대시보드 → My Profile → API Tokens → 권한 "Workers KV
  Storage: Edit" 로 토큰 생성 → `scripts/env.local`(gitignored) 에
  `export CLOUDFLARE_API_TOKEN=...` 한 줄. 인증 전엔 `maint on` 이 서버 쪽 플래그만 세우고
  안내 페이지는 못 켭니다 — 메시지로 알려줍니다.
- `deploy.sh status` 에 `[maint]    ON` 이 보이면 **끄는 걸 잊은 것**입니다. 켜져 있는 동안
  헬스체크가 죽은 서버를 살리지 않습니다.

## 자주 묻는 것

- **머지했는데 사이트가 안 바뀌어요.** 머지 ≠ 배포입니다. 성원의 릴리스 발행 + 진서의
  `deploy.sh` 까지 해야 바뀝니다.
- **지금 뭐가 떠 있어요?** `./scripts/deploy.sh status`, 또는 GitHub **Releases** 의 최신
  pre-release. 둘이 다르면 status 가 경고합니다.
- **급한 버그(핫픽스)는?** 똑같습니다: 브랜치 → PR → 머지 → 성원 릴리스(`v0.2.1-alpha`) →
  배포. 성원과 연락이 안 되면 진서가 대신 발행하되, 릴리스 설명 첫 줄에
  "긴급 — 성원 부재로 진서 발행, 사후 확인 요청" 을 남깁니다. (예외는 기록이 남아야 예외입니다.)
- **롤백하면 KB 데이터(인덱스)는?** `qdrant_db/`·`parent_store/` 는 레포에 같이 있어서 코드와
  함께 그 태그 시점으로 돌아갑니다.
- **개발 폴더(`~/camchat`)에서 브랜치를 바꿔도 되나요?** 됩니다. 운영은 `~/camchat-prod`
  의 다른 프로세스라 개발 폴더에서 뭘 하든 사용자한텐 영향 없습니다. 반대로 운영·staging 폴더에서는
  아무것도 바꾸지 마세요 — 커밋은 훅이 막고, 편집은 다음 배포를 막습니다.
- **`deploy.sh` 가 "systemd 가 서비스하는 체크아웃은 … 인데 여기는 …" 하고 거부해요.** 개발
  폴더에서 쳤습니다. `cd ~/camchat-prod` 에서.
- **`deploy.sh` 가 "uncommitted changes" 라고 거부해요.** 운영 폴더에 커밋 안 된 수정이
  있습니다. 운영은 태그와 정확히 같아야 하므로 커밋(PR)하거나 `git stash` 한 뒤 다시.

## 아직 없는 것 (다음 단계, 필요해지면)

- **staging 자동 갱신** — 지금은 `staging.sh up` 을 손으로. 머지될 때마다 자동으로 띄우려면 타이머 하나.
- **운영 전용 `.venv`** — 지금은 개발 폴더 것을 공유. 의존성이 자주 바뀌기 시작하면 분리.
