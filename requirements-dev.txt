# Unit-test runtime (fully self-sufficient — no torch/sentence-transformers needed)
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
pydantic
langgraph==1.2.6
langchain-text-splitters==1.1.2
tiktoken==0.13.0

# debug/ toolkit import-time deps (needed to collect tests/debug/*; the heavier
# deps — langfuse, qdrant-client, torch — are imported lazily and stay optional)
python-dotenv
requests

# Integration-test extras (install separately — not required for offline unit suite)
# pip install langchain-ollama==1.1.0  qdrant-client  langfuse==4.6.1

# Korean sparse-tokenizer A/B (db/korean_sparse.py). kiwipiepy (SPARSE_MODEL=kiwi, the
# default) is a runtime dep in requirements.txt. The optional OKT strategy needs a JVM:
# pip install konlpy jpype1        # SPARSE_MODEL=okt  (requires a JDK + JAVA_HOME, e.g. Temurin 17)
