# BUFS Chatbot v2 — 작업 지침

부산외대 학사 RAG 챗봇 (베이스라인 이전 버전). 자세한 내용은 `README.md` 참조.

## ⛔ 절대 규칙 — 깃 작업 방식 (위반 금지)

**전체 절차는 `WORKFLOW.md`를 따른다. 핵심 하드룰은 아래 4개:**

1. **`main`은 운영/배포 서버 코드다. 절대 직접 작업·커밋·push 하지 않는다.**
   무슨 일이든 **새 브랜치부터** 판다. (사용자가 "main에 커밋해"라고 명시적으로
   지시하지 않는 한, 에이전트는 main에 커밋하지 말 것.)
2. **작업 시작 = 최신 `main`에서 새 브랜치.** 작업이 길어지면 `git fetch origin` 후
   `origin/main`을 내 브랜치에 합쳐 충돌을 미리 푼다. PR 올리기 직전에도 한 번 합친다.
3. **작업이 끝나면 `main`으로 PR을 올린다.** PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md`
   양식을 채운다(특히 "정답률 결과" 칸). push는 사용자 명시 지시가 있을 때만.
4. **다음 작업은 머지를 기다리지 말고, 다시 최신 `main`에서 새 브랜치를 판다.**
   이전 브랜치 위에 다음 작업을 얹지 않는다.

> 생성물(데이터·인덱스·평가 리포트)은 커밋하지 않는다 — `.gitignore`에 맡긴다.

## 최초 1회 셋업 (각자, 또는 에이전트한테 시키기)

```bash
git config core.hooksPath .githooks   # main 직접 커밋 차단 훅 활성화
```

---

(프로젝트별 아키텍처·검색 우선순위·평가 기준 등 상세 지침은 베이스라인 확정 후 추가)
