# BUFS Chatbot v2 — 작업 지침

부산외대 학사 RAG 챗봇 (베이스라인 이전 버전). 자세한 내용은 `README.md` 참조.

## ⛔ 절대 규칙 — 깃 작업 방식 (위반 금지)

**전체 절차는 `WORKFLOW.md`를 따른다. 핵심 하드룰은 아래 4개:**

1. **`main`은 운영/배포 서버 코드다. 절대 직접 작업·커밋·push 하지 않는다.**
   무슨 일이든 **새 브랜치부터** 판다. (사용자가 "main에 커밋해"라고 명시적으로
   지시하지 않는 한, 에이전트는 main에 커밋하지 말 것.)
2. **작업 시작 = 최신 `main`에서 새 브랜치.** 그리고 **세션 시작 시 / 작업 중 주기적으로 /
   PR 올리기 직전에** `main`과 동기화해 충돌을 미리 드러낸다 (오래 작업해도 PR 시점 충돌 최소화):
   - feature 브랜치에서 `git fetch origin` 후 `git rev-list --count HEAD..origin/main`로
     main이 앞서갔는지 확인. **앞서갔고 작업트리가 깨끗하면** `bash .claude/scripts/sync-main.sh`
     (= `git merge origin/main`)로 합친다.
   - **충돌이 나면 즉시 사용자에게 알리고 함께 해결**한다(임의로 한쪽을 버리지 않는다).
   - 미커밋 변경이 있으면 먼저 커밋/스태시 후 동기화. `main` 브랜치에서는 하지 않는다.
3. **작업이 끝나면 `main`으로 PR을 올린다.** 단, **PR 올리기 직전에 `/code-review`를 한 번
   돌려 지적사항을 반영한다.** PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md` 양식을 채운다
   (특히 "정답률 결과" 칸). push는 사용자 명시 지시가 있을 때만.
4. **다음 작업은 머지를 기다리지 말고, 다시 최신 `main`에서 새 브랜치를 판다.**
   이전 브랜치 위에 다음 작업을 얹지 않는다.

> 생성물(데이터·인덱스·평가 리포트)은 커밋하지 않는다 — `.gitignore`에 맡긴다.

## 에이전트 자가 셋업 (세션 시작 시 확인)

작업을 시작하기 전에, 이 레포의 훅이 켜져 있는지 확인하고 안 켜져 있으면 켠다:

```bash
[ "$(git config --get core.hooksPath)" = ".claude/githooks" ] || bash .claude/scripts/setup-dev.sh
```

사용자가 "셋업해줘"라고 하면 `bash .claude/scripts/setup-dev.sh`를 실행한다
(main 커밋 차단 훅 + 줄바꿈 정규화). 자세한 작업 절차는 `WORKFLOW.md`,
OS·줄바꿈·인덱스 데이터 주의는 `WORKFLOW.md`의 "Windows/Mac 참고" 참조.

---

## 이 프로젝트가 뭔지 (1줄)

`agentic-rag-for-dummies`의 LangGraph 에이전트 코어(`project/`)에 CamChat 채팅 UI(`frontend/`)를
얹은 부산외대 학사 Q&A 챗봇. **아키텍처·실행 방법·성능 수치는 `README.md`가 정본(SSOT)이다 —
여기서 중복 설명하지 않는다.** 협업·깃 절차는 위 하드룰 + `WORKFLOW.md`.

## 핵심 원칙 (3)

1. **하드코딩 금지 — `project/config.py`가 설정 SSOT.** 모델명·경로·임계치·루프 상한은 전부
   `config.py`의 env 기반 상수로 이미 노출돼 있다. 코드에 매직 넘버/절대경로를 박지 말고
   거기 추가한다(롤백·튜닝이 코드 변경 없이 되도록).
2. **비용·지연을 의식한다.** 에이전트는 한 턴에 LLM을 4~7회 부른다(orchestrator 루프 +
   compress). 지연의 원천이 여기다 — `MAX_TOOL_CALLS`·`BASE_TOKEN_THRESHOLD`·`LLM_NUM_CTX`로
   조절하되, 검색 깊이(정답률)와 트레이드오프임을 기억한다.
3. **생성물은 커밋하지 않는다(코드 PR에).** 인덱스(`qdrant_db/`·`parent_store/`)·평가 리포트·
   로그는 `.gitignore`에 맡긴다. 인덱스 재생성은 **문서가 바뀐 경우에만**, "인덱스 갱신" **별도 PR**로.

## 변경 전 알아둘 비자명 함정

- **검색 임계치 `SEARCH_SCORE_THRESHOLD`는 0.3** — 하이브리드(RRF) 점수는 코사인 유사도가
  아니라 *rank* 점수(top≈0.5)라, 원본 기본값 0.7이면 전부 거부된다. cosine으로 착각 금지.
- **Ollama는 thinking off가 기본**(`LLM_REASONING=false`). qwen3 계열은 추론 토큰이
  스트리밍 답변에 새어 들어가고 느려진다. thinking 모델로 바꾸지 말 것.
- **임베딩 모델(`DENSE_MODEL`)을 바꾸면 벡터 차원이 바뀐다** → 기존 컬렉션과 불호환.
  반드시 `python project/ingest.py --clear`로 재인제스트.
- **KB는 정적**이다 — `markdown_docs/` 학사문서 23종 + 커밋된 인덱스. 라이브 크롤러는 없다.

## 정답률 게이트 (검색·생성·프롬프트·에이전트 루프 변경 시 필수)

> **에이전트 지시(soft-enforcement):** 모든 팀원이 Claude로 작업하므로 이 게이트는 코드가 아니라
> 이 규칙으로 강제한다. 검색·생성에 영향 주는 변경이면 **에이전트는 PR 전에 반드시 아래를 수행**한다.

이 영역에 영향 주는 변경(파이프라인·프롬프트·`config.py` 검색/에이전트 값·청킹)은 PR 전:

1. `eval_tools/`의 combined88(89문항: 답변가능 81 + 문서밖 8) 룰기반 평가를 돌린다.
2. 결과를 PR 템플릿의 **"정답률 결과"** 칸에 적는다(`.github/PULL_REQUEST_TEMPLATE.md`).
3. **NO-GO**(아래 중 하나면 커밋·PR 보류, 원인 분석 후 @JinnyLyn에게 보고):
   - 전체 contains 정답률 **-1pp 이상** 회귀, 또는
   - 단일 데이터셋 **-3pp 이상** 회귀, 또는
   - 문서밖 거부율 폭락.
- **현재 baseline**: `REPORT_결과.md` 기준 룰기반 contains **85.2%**, 문서밖 거부 **8/8(100%)**.
- ⚠️ **주의(harness 미정비)**: `eval_tools/_eval_combined88.py`는 현재 **Windows 절대경로**
  (`C:\Users\...`, 레거시 레포의 데이터셋·출력)가 하드코딩돼 있어 **이 레포/다른 OS에서 바로 안 돈다.**
  게이트를 돌리려면 먼저 `SRC`/`OUT`/`JSONL` 경로를 레포 상대경로로 고쳐야 한다(별도 정비 과제).
- **영향 없는 변경**(UI 텍스트·주석·로그 메시지 등)은 게이트 생략 가능 — **사유 한 줄** 명시.
- **못 돌리는 상황이면 임의로 생략하지 않는다.** 백엔드·Ollama·인덱스가 안 떠 있어서 평가를
  못 돌리면, 조용히 넘기지 말고 PR에 **"평가 못 돌림 + 이유"**를 적고 사용자에게 알린다.
- **최종 백스톱(사람):** 리뷰어(@JinnyLyn)는 영향 있는 변경인데 **"정답률 결과" 칸이 비어 있으면
  머지하지 않는다.** 이게 유일한 하드 게이트다.

---

> 이 파일은 **모든 팀원이 공유**하는 지침이다(Mac·WSL·Claude Desktop 혼용). OS·머신 특정 경로나
> 가정을 넣지 말 것. 새 규칙은 "지금 사실이고 비자명하며 실수를 막는" 것만 추가하고, 그 외엔
> 실제로 막혔을 때 한 줄씩 늘린다.
