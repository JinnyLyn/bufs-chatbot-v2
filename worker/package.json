{
  "name": "camchat-outage-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\"",
    "check": "npx wrangler deploy --dry-run --env \"\"",
    "deploy:preview": "npx wrangler deploy --env \"\"",
    "deploy": "npx wrangler deploy --env production",
    "tail": "npx wrangler tail --env production --format pretty",
    "maintenance:on": "npx wrangler kv key put --binding OUTAGE --remote --env production maintenance on",
    "maintenance:off": "npx wrangler kv key delete --binding OUTAGE --remote --env production maintenance",
    "maintenance:status": "npx wrangler kv key get --binding OUTAGE --remote --env production maintenance"
  }
}
