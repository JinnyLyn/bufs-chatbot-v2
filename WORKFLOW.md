# 팀 작업 방식 (WORKFLOW)

> 이 문서 하나면 됩니다. **막히면 리뷰 담당(@JinnyLyn)한테 물어보세요.**

---

## ⛔ 제1원칙: `main`은 절대 직접 건드리지 않는다

**`main` = 지금 돌아가는 운영/배포 서버 코드입니다.** 여기에 직접 커밋하거나 작업하면
실서비스가 깨질 수 있어요. 그래서 규칙은 딱 하나로 시작합니다:

> **무엇을 하든, 일단 새 브랜치부터 판다. `main`에서 직접 작업 금지.**

---

## 🔧 최초 1회만 셋업

레포 받은 직후 한 번만 실행하면, `main`에 실수로 커밋하는 걸 자동으로 막아줍니다:

```bash
git config core.hooksPath .githooks
```

> 자연어로 시키는 분은: **"`git config core.hooksPath .githooks` 실행해줘"** 라고 한 번 말하면 됩니다.

---

## 🔁 작업 한 사이클 (이 4단계만 반복)

```
 ① 시작        ② 작업 중           ③ 끝           ④ 다음 일
 main 최신화 →  가끔 main 받아 합침 → PR 올림  →  다시 ①로
 새 브랜치 팜                       (템플릿 채움)   (새 브랜치)
```

### ① 시작할 때 — main 최신화하고 새 브랜치 파기
- 항상 **최신 `main`에서** 브랜치를 따요. (안 그러면 남이 한 작업이랑 충돌나요)
- 브랜치 이름 예: `feat/검색-속도개선`, `fix/로그인-버그`, `docs/...`

### ② 작업하는 도중 — 가끔 main을 받아서 내 브랜치에 합치기
- 작업이 길어지면, 그사이 `main`이 앞서갑니다. **하루 한 번쯤, 그리고 PR 올리기 직전에**
  최신 `main`을 받아 내 브랜치에 합쳐두세요. → 충돌을 **조금씩 미리** 풀어서 나중에
  큰 충돌이 안 나요.

### ③ 끝나면 — PR 똑바로 올리기
- 내 브랜치를 **`main`으로** PR(합쳐달라는 요청) 올립니다.
- **PR 양식(템플릿)을 채워요.** "뭘/왜 바꿨는지", 검증했는지를 적습니다.
  (양식은 PR 만들 때 자동으로 떠요. 자세한 건 아래 "PR 올리기" 참고)
- 리뷰 담당(@JinnyLyn)이 자동으로 배정돼서 확인합니다.

### ④ 다음 일 — **머지를 기다리지 말고**, 다시 `main`에서 새 브랜치
- PR 올렸으면 그 브랜치는 거기서 끝. 다음 작업은 **①로 돌아가** 최신 `main`에서
  **새 브랜치**를 또 팝니다.
- ❌ **앞 브랜치 위에 얹어서 다음 작업 하지 마세요.** (이게 지난번 충돌 대란의 원인)

---

## 💻 복붙용 명령어 (git 직접 치는 사람)

```bash
# ① 시작: main 최신화 + 새 브랜치
git checkout main
git pull origin main
git checkout -b feat/내-작업이름

# ... 작업 + 커밋 ...
git add -p
git commit -m "feat: 무엇을 했는지"

# ② (작업 길어지면 / PR 직전) main 받아 합치기
git fetch origin
git merge origin/main          # 충돌 나면 풀고 다시 commit

# ③ 끝 → 올리고 PR 생성
git push -u origin feat/내-작업이름
gh pr create --base main --fill   # 그다음 PR 본문에 템플릿 채우기

# ④ 다음 작업 → ①로 (다시 main에서 새 브랜치)
```

---

## 🗣 자연어로 시키는 사람용 (Claude Desktop 등, git 직접 안 치는 경우)

git 명령을 직접 안 쳐도 됩니다. **에이전트한테 이렇게 말하세요** (그대로 복사해서 써도 됨):

- 시작할 때:
  > "main 최신으로 받고, `feat/내-작업이름` 새 브랜치 파서 거기서 작업하자. main에서 직접 작업하지 마."
- 작업 도중:
  > "최신 main 받아서 지금 브랜치에 합쳐줘. 충돌 있으면 알려줘."
- 끝나면:
  > "이 브랜치 push하고 main으로 PR 올려줘. PR 템플릿 칸 다 채워줘."
- 다음 작업:
  > "이건 새 작업이니까 main에서 새 브랜치 파서 시작하자."

> 핵심만 기억: **"항상 새 브랜치에서. main 직접 금지. 끝나면 PR."** 이 말만 해주면 됩니다.

---

## 🚫 하지 말 것

- ❌ `main`에서 직접 작업 / 커밋 / push  → **운영 서버라 금지**
- ❌ 머지 안 된 브랜치 위에 다음 작업 얹기  → 브랜치 엉키고 충돌
- ❌ 생성물(데이터·인덱스·평가 리포트) 커밋  → `.gitignore`에 맡기기. PR이 수만 줄로 부풀어요
- ❌ PR 올려놓고 양식 비워두기  → 리뷰어가 뭘 확인해야 할지 모름

## 🧯 충돌(conflict) 났을 때

당황하지 마세요. 작업이 사라지는 게 아니에요.
1. `git status`로 충돌 파일 확인 → 표시된 부분 직접 정리
2. 모르겠으면 **그대로 두고 @JinnyLyn 호출** (또는 에이전트한테 "충돌 어떻게 풀지 설명해줘")
3. 푼 뒤 `git add` → `git commit` → 계속 진행

---

## 왜 이렇게까지 하나 (한 줄)

지난 레포에서 "브랜치 끝내고 머지 안 하고 다음 브랜치를 그 위에 파는" 방식 때문에
**PR 5개 중 4개가 충돌**나고 "문서 1개 추가" PR이 1.7만 줄로 부푼 사고가 있었어요.
이 4단계만 지키면 그게 안 생깁니다.
