# 부산외대 학사 챗봇 — 테스트 결과 보고서

> 작성: 2026-06-08 · 대상: agentic-RAG 융합 챗봇 (CamChat 프론트 + agentic-rag 백엔드)
> 평가: 룰기반(combined88) + RAGAS(LLM-judge) + Langfuse 로그 체계분석

---

## 1. 시스템 개요

| 항목 | 내용 |
|---|---|
| 아키텍처 | agentic-RAG (LangGraph): `rewrite → agent(orchestrator·search·parent·compress) → aggregate`, FastAPI SSE + Next.js |
| LLM | **qwen3.5:9b** (Q4_K_M) on **원격 H100** (SSH 터널 :11434, ~59 tok/s) |
| 검색 | Qdrant 임베디드, **하이브리드**(dense `bge-m3` + sparse BM25, RRF), parent/child 청킹 |
| 지식베이스 | BUFS 2026-1 학사문서 **23개** (학사안내·수강신청·장학·학생포털 등), 1,459 child / 194 parent |
| 관측 | Langfuse Cloud(전 요청 추적) + 파일로그 + Q&A JSONL + `/health` |
| 평가구성 | H100 + `num_ctx=16384` + 무압축(`BASE_TOKEN_THRESHOLD=12000`) + 빠른거부 |

테스트셋: bufs-chatbot의 `combined88` (89문항 = 답변가능 81 + 문서밖 8), 각 문항에 정답(ground_truth) + 핵심팩트 보유.

---

## 2. 룰기반 결과 (89문항)

채점: 정답의 핵심팩트(숫자·날짜·등급)가 답변에 포함됐는지 — **자릿수 경계 매칭**(`6`≠`16`), 날짜 표면형 변환, 24/12시 등가, "불가능" 같은 단어로 정답을 거부로 오인하지 않도록 보정.

| 지표 | 값 | 비고 |
|---|---|---|
| **contains 정답률** (답변가능 81) | **85.2%** (69/81) | bufs 80.3% 상회 |
| strict (핵심팩트 전부일치) | 66.7% (54/81) | |
| **문서밖 거부율** (8문항) | **100%** (8/8) | bufs 50% |

### intent별 contains
| intent | 정답/전체 |
|---|---|
| GRADUATION_REQ | 5/7 |
| SCHEDULE | 25/26 |
| REGISTRATION | 25/28 |
| MAJOR_CHANGE | 5/6 |
| SCHOLARSHIP | 3/4 |
| OCU·기타 | 대부분 정답 |

---

## 3. 지연(latency) 결과 — 89문항 실측(duration_ms)

| | 값 |
|---|---|
| 평균 | **10.8초** |
| median(p50) | 9.3초 |
| p90 | 16.9초 |
| p95 | 18.8초 |
| **최대** | **28.6초** |
| 100초 초과 | **0건** |
| 60초 초과 | **0건** |

→ **모든 응답이 30초 이내. Cloudflare 터널 100초 제한을 절대 넘지 않아 배포 안전.**

---

## 4. RAGAS 결과 (25문항, LLM-judge=exaone3.5:7.8b)

생성=H100 qwen3.5:9b, 평가=로컬 exaone(자기편향 회피). bufs와 동일 5지표·동일 judge 계열.

| 지표 | 점수 | 막대 |
|---|---|---|
| Faithfulness 성실성 | **0.836** | 답변이 컨텍스트에 충실(낮은 할루시네이션) |
| Answer Relevancy 관련성 | **0.78** | 질문 의도 부합 |
| Context Precision 정밀도 | **0.632** | 검색 컨텍스트 노이즈 적음 |
| Context Recall 재현율 | **0.68** | 정답 근거의 검색 포함도 |
| Answer Correctness 정확도 | **0.58** | 정답과 일치도 |
| **종합 AVG** | **0.702** | |

> 주의: RAGAS 절대점수는 judge에 좌우됨(exaone≈0.70, gemini-2.5-flash는 더 깐깐, gemini-1.5-flash는 더 후함). 비교는 동일 judge끼리만 유효.

---

## 5. Langfuse 로그 체계분석

전 요청(252+ 트레이스)을 Langfuse REST API로 집계.

### 5-1. 파이프라인 건강
- **에러/경고 관측 0건** — 실패한 LLM 호출·예외·structured-output 파싱 에러 없음. 파이프라인 안정.

### 5-2. 정답/오답 귀인 (정답 근거가 컨텍스트에 있었나로 분류)
| 분류 | 비율 | 의미 |
|---|---|---|
| CORRECT | **74%** | 정답 |
| GENERATION_ERR | 10% | 근거는 컨텍스트에 **있었으나** LLM이 틀림 |
| RETRIEVAL_ERR | 16% | 근거가 검색 안 됨 |

- **생성오류**(q019 22→21, q035 OCU 6→18, q036): 여러 비슷한 값이 든 **표에서 LLM이 엉뚱한 행 선택**. 검색은 성공.
- **검색오류 16%**: 현재 "빠른" 구성이 큰 컨텍스트로 **툴호출을 덜 해서**(검색깊이↓) 근거를 덜 가져옴 + 학사일정 날짜는 표 형태라 매칭기 한계도 일부 포함.

> RAGAS의 Faithfulness 높음(0.84) + Correctness 낮음(0.58)과 정확히 일치: **근거에는 충실하나, 비슷한 값 중 정확한 것을 고르는 정밀도가 약점.**

---

## 6. 주요 개선 이력과 효과

| 수정 | 문제 | 효과 |
|---|---|---|
| **학번 섹션 청킹** | 학번별 졸업요건이 인접 학번과 섞임 (2020 졸업 "정보없음") | 2020/2022 졸업학점 정확 추출, GRADUATION 정상화 |
| **연도 펼치기** | "2020학번"이 "2017~2020학번" 헤더와 매칭 안 됨 | 단일연도 질의가 범위 섹션에 매칭 |
| **졸업학점 합산금지 프롬프트** | 2022 졸업을 구성요소 합산 "107학점" 오답 | **107→130** 정정 |
| **학사일정 월 채우기** | 표의 빈 '월' 칸 → 기말고사 "6/8"을 "5/8"로 | 월 전부 정정 |
| **클린 리인덱스** | Qdrant에 유령 11,939청크(69출처) 누적 | **1,459청크/23출처**로 정리 |
| **H100 + 무압축** | 압축 노드가 매 툴호출마다 ~20초 요약 3회 | 2020질의 **103초→30초** |
| **빠른거부** | 문서밖 질문이 290초 폭주 | **290초→10초, 거부율 5/8→8/8** |

---

## 7. 핵심 트레이드오프 — 두 운영점

| | **A. 정확도 우선** (로컬+압축) | **B. 속도/배포 우선** (H100+무압축+빠른거부) ★현재 |
|---|---|---|
| contains | **91.4%** | 85.2% |
| strict | 76.5% | 66.7% |
| RAGAS avg | 0.724 | 0.702 |
| 거부율 | 5/8 | **8/8** |
| 평균 지연 | ~20초 | **10.8초** |
| 최대 지연 | **137초** ❌ | **28.6초** ✅ |
| 배포(Cloudflare) | 일부 524 타임아웃 | **안전** |

**원인**: B 구성은 큰 컨텍스트(16k)로 오케스트레이터가 1회 검색으로 빠르게 판단 → 속도↑·꼬리 제거, 그러나 검색깊이↓로 정확도 ~6pp 손해. 두 구성 모두 bufs(80.3%)보다 정확.

---

## 8. 알려진 한계 & 다음 단계

- **표 행 선택 정밀도**(생성오류 10%): 비슷한 값 표에서 LLM이 잘못된 행 선택(인접 시험기간, OCU vs 일반, 일부 학번). → 표 disambiguation 프롬프트 / 행단위 청킹.
- **속도↔정확도**: 배포는 B 권장(안전), 정밀 답변이 중요하면 H100에서 압축 유지+적정 num_ctx로 **양쪽 절충 구성** 실험 여지.
- **KB 커버리지**: 23문서 한정 → 문서밖 질문은 정직 거부(8/8). 범위 확대 시 recall↑.
- **공유 H100 의존**: SSH 터널·원격 가용성에 종속. 로컬 4070 폴백(`backups/.env.bak-local-gpu`) 보유.

## 9. 배포 준비도
✅ 응답 100% 30초 이내 · ✅ 문서밖 100% 거부 · ✅ 0 파이프라인 에러 · ✅ contains 85% (>bufs) · ✅ 롤백 스크립트 완비
→ **maruvis.co.kr 배포 가능 상태** (남은 단계: `scripts/apply-maruvis-tunnel.ps1` 관리자 실행).
