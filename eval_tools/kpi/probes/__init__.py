"""eval_tools.kpi.probes — Real-Usage Robustness Suite component probes.

Placeholder package for the Phase-1B probes (WS-R4): ``clarity`` (is_clear
gate), ``tokenizer`` (kiwi robustness), ``rewrite`` (rewrite_query drift), and
``refuse`` (fast-refuse boundary). Empty in the foundation commit.
"""
