# Scorer Equivalence Matrix

Auto-generated by `eval_tools/kpi/_equivalence_matrix.py`.
Reference (canonical): **`eval_tools.kpi.scorer`** — the corrected lineage.
Corpus: `tests/kpi/fixtures/combined88_new_result.json`
(89 records: 81 answerable, 8 unanswerable).

## Summary

| Status | Count |
|--------|-------|
| IDENTICAL | 4 |
| DIVERGE | 1 |
| uncomparable (side-effect import / heavy dep) | 8 |

## Per-Module Matrix

| Module | `extract_facts` | `matched` | `is_refusal` | Overall |
|--------|----------------|-----------|--------------|---------|
| `_eval_combined88` | — | — | — | uncomparable: FileNotFoundError: [Errno 2] No such file or directory: '<windows-path>' |
| `_eval88_rich` | — | — | — | uncomparable: FileNotFoundError: [Errno 2] No such file or directory: '<windows-path>' |
| `_eval_runner` | — | — | — | uncomparable: FileNotFoundError: [Errno 2] No such file or directory: '<windows-path>' |
| `_rescore88` | IDENTICAL (81 items) | IDENTICAL (81 items) | N/A (no fn) | **IDENTICAL** |
| `_retrieval_recall` | — | — | — | uncomparable: ModuleNotFoundError: No module named 'langchain_huggingface' |
| `_aggregate_variants` | IDENTICAL (81 items) | IDENTICAL (81 items) | IDENTICAL (89 items) | **IDENTICAL** |
| `_test_fastrefuse` | — | — | — | uncomparable: FileNotFoundError: [Errno 2] No such file or directory: '<windows-path>' |
| `_stage_attribution` | DIVERGE (1 items; e.g. q027) | DIVERGE (1 items; e.g. q011) | N/A (no fn) | **DIVERGE** |
| `_compare_ba` | IDENTICAL (81 items) | IDENTICAL (81 items) | IDENTICAL (89 items) | **IDENTICAL** |
| `_compare_h100` | IDENTICAL (81 items) | IDENTICAL (81 items) | IDENTICAL (89 items) | **IDENTICAL** |
| `_answer_analysis` | — | — | — | uncomparable: FileNotFoundError: [Errno 2] No such file or directory: '<windows-path>' |
| `_verify_fixes` | — | — | — | uncomparable: FileNotFoundError: [Errno 2] No such file or directory: '<windows-path>' |
| `_check2020` | — | — | — | uncomparable: ConnectionError: HTTPConnectionPool(host='localhost', port=8000): Max retries ex |

## Notes on Divergent Module

**`_stage_attribution`** diverges on 2 items (q027 `extract_facts`, q011 `matched`) for
two reasons that are not bugs — they reflect its different design purpose:

1. `extract_facts` uses the old `[A-F]\+` regex (D1 bug, misses bare single-letter grades
   like `A` or `B`), causing a divergence on records whose ground-truth contains only a
   bare grade letter without a `+`/`-` suffix.
2. `matched` adds extended table-cell date patterns (`| 6 | 8...` formats) not present in
   the canonical — this is intentionally broader for stage-attribution analysis of
   rich KB-corpus text, and can match items the canonical would not.

Neither is a regression in the canonical scorer.

## Known Deltas (buggy combined88 → corrected lineage)

Three pre-classified bugs distinguish `_eval_combined88` from the corrected lineage (`_rescore88` / `_aggregate_variants` / `eval_tools.kpi.scorer`):

| ID | Description | Affected function |
|----|-------------|-------------------|
| **D1** | Grade regex `[A-F]\+` misses bare single-letter grades (`A`, `B`, …). Corrected: `(?<![A-Za-z])[A-F][+-]?(?![A-Za-z])`. | `extract_facts` |
| **D2** | 24h↔12h time equivalence missing: `13` should also match `오후 1시`. Corrected adds `13 ≤ n ≤ 23` branch in `matched`. | `matched` |
| **D3** | `is_refusal` subtraction applied to *answerable* items — a correct answer containing '불가능' was incorrectly scored as refusal. Corrected lineage applies refusal markers **only to unanswerable** items. | `is_refusal` (usage) |

## Canonical = Corrected Lineage

`eval_tools.kpi.scorer` is a logic-preserving port of `_rescore88.py` / `_aggregate_variants.py` (AC#1 0-diff parity verified by `tests/kpi/test_scorer_parity.py`). Modules listed as UNCOMPARABLE could not be imported without side effects (hardcoded Windows file paths, live network calls, or heavy non-stdlib deps) — this is expected for ad-hoc analysis scripts; it is not a defect in the canonical scorer.
